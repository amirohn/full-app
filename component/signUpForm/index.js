import {FormParent} from "./SignUpForm.styled";
import {Button} from "@mui/material";

export const SignUpForm = () => {
  return (
    <>
      <FormParent>this is a test</FormParent>
      <Button variant="contained" color="primary">
          click me
        </Button>
    </>
  );
};
